name: Keep GeneLens Alive

on:
  schedule:
    - cron: '0 */6 * * *'  # runs every 6 hours
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping GeneLens
        run: curl -s https://genelens.streamlit.app/ > /dev/null
